require('crypto').randomBytes(64).toString('hex'); -> generate token

truncate albums, songs, users, authentications, playlists, playlist_songs, collaborations, playlist_song_activities, user_album_likes;